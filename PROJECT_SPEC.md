# PotatoEye 马铃薯切块种薯入催芽室联检闭环

## 项目目标

PotatoEye 是单节点 Go HTTP 后端，围绕一次马铃薯切块种薯批进入催芽室前的联检流程建立确定状态机。系统在锁定阶段冻结繁育地块、品种代次、原薯窖位摘要、切刀消毒批次、切块批号、网筐封签、愈伤棚位、温湿度探头窗口、抽样盲码、芽眼覆盖点位、快检板孔、催芽室时隙、木栓化阈值、失水率阈值、病害阈值、复核人员名单和任务代次；随后推进双人切块确认、网筐样本封存、资源租约占用、按日龄愈伤观察、芽眼活性核验、病害复测、异常筐位复判、独立复核，最终只形成允许入催芽、病害隔离或任务取消中的一个终局。SQLite WAL 持久化目录、任务聚合、资源租约、覆盖矩阵、不可覆盖证据版本链、适配器调用、幂等结果、审计事件和终局凭据，进程重启后可确定恢复。规模目标为 24-28 个生产 Go 文件、约 2400-2700 行有效生产 Go 代码，控制在 2000-3000 行，覆盖 catalog、task、ledger、evidence、arbiter、store、api、cmd 等包，并提供 go.mod 与 linux/amd64、linux/arm64 双架构 Docker 构建。

## 端到端业务流程

1. 建档锁定：提交繁育地块、品种代次、原薯窖位摘要、切刀消毒批次、切块批号、网筐封签集合、愈伤棚位、探头窗口、抽样盲码、芽眼点位、快检板孔、催芽室时隙、阈值快照和复核人员名单；服务校验目录一致性、资源可用性和重复键后生成任务代次。
2. 双人切块确认：两名不同合格人员分别提交当前代次确认，确认内容必须匹配锁定快照；同一操作号同内容重试返回既有结果，内容冲突返回稳定错误码。
3. 样本封存与占用：为每个网筐封签绑定盲码和留样记录，原子占用愈伤棚位、探头窗口、快检板孔和催芽室时隙；任何冲突都会回滚整次写入。
4. 愈伤观察推进：按锁定日龄提交覆盖矩阵，记录木栓化等级、失水块数、腐烂块数、块重变化、棚温、相对湿度和缺测原因；缺失、重复、越界或不守恒计数不得进入有效覆盖格。
5. 芽眼与病害核验：按网筐、盲码、日龄和点位记录芽眼活性计数，并接收软腐与晚疫病快检孔位读数；所有理化和快检值使用固定小数位整数运算并写入不可覆盖证据版本链。
6. 复判与独立复核：阈值异常只能创建一个带代次的复判证据，复判覆盖受影响日龄、网筐、盲码和孔位；两名不同合格人员完成独立复核后，终局仲裁器以单写屏障产生唯一结论。

## 核心组件与职责

1. 种薯品种与愈伤规则目录：维护繁育地块、品种代次、原薯窖位摘要、切刀消毒批次、阈值规则、合格人员角色和规则版本。
2. 切块入催芽任务聚合：实现待锁定、待切块确认、样本封存中、棚位占用中、愈伤观察中、芽眼核验中、病害复测中、待独立复核、可入催芽、已入催芽、病害隔离和已取消状态机。
3. 网筐样本与资源占用账簿：管理切块批号、网筐封签、盲码、愈伤棚位、探头窗口、催芽室时隙和检测孔位的一次性锁定及并发租约。
4. 愈伤日龄及芽眼采集账簿：保存按日龄、网筐、点位展开的木栓化、失水、腐烂、芽眼活性和补测标记覆盖矩阵。
5. 病害复判及终局仲裁器：保存快检、病斑、渗液、理化证据版本链，执行异常复判、独立复核和终局单写屏障。
6. Go HTTP API：提供 JSON 接口、稳定错误码、幂等操作号处理、适配器故障脚本入口、逻辑时钟注入和审计查询。

## 领域规则与不变量

1. 锁定后冻结所有关键标识、资源窗口、阈值快照、复核名单和任务代次，后续推进必须携带当前代次。
2. 同一开放任务集合中，切块批号、网筐封签、盲码、愈伤棚位、探头窗口、催芽室时隙和检测孔位只能被一个有效任务持有。
3. 繁育地块与品种代次必须匹配目录，原薯窖位摘要和切刀消毒批次必须等于锁定时校验通过的版本。
4. 盲码在揭示前只能作为样本索引，提前揭示、重复盲码或揭示内容与封存样本不一致均拒绝。
5. 愈伤观察必须覆盖锁定日龄和网筐集合；木栓化等级、失水块数、腐烂块数、芽眼活性和补测标记必须满足整数守恒规则。
6. 失水率、棚温、相对湿度、块重变化、快检 Ct 值和病斑直径使用定点整数，检查长度、符号、除零和溢出。
7. 快检仪、温湿度探头和称重适配器的拒绝、断连、超时或格式错误只形成待重试调用记录，不生成合格证据，不提前释放占用。
8. 终态形成后，迟到读数、复判、复核或普通推进请求一律返回稳定错误码且不改变状态。

## 数据模型与持久化

1. catalog_rules：地块、品种代次、窖位摘要、消毒批次、阈值、观察日龄、人员资格和规则版本。
2. tasks：任务 ID、切块批号、状态、任务代次、锁定快照摘要、创建逻辑时间、终局摘要。
3. basket_samples：任务 ID、网筐封签、盲码、封存状态、揭示状态、样本代次。
4. resource_leases：资源类型、资源键、任务 ID、代次、租约状态、开始和结束逻辑时间，用唯一约束保护开放占用。
5. observations：任务 ID、日龄、网筐封签、点位、木栓化等级、失水块数、腐烂块数、芽眼活性、补测标记、证据版本。
6. disease_evidence：任务 ID、盲码、孔位、病斑直径、渗液等级、Ct 定点值、复判代次、来源调用 ID、不可覆盖版本。
7. idempotency_records：操作号、请求摘要、响应摘要、任务代次、逻辑时间和冲突标记。
8. adapter_calls、reviews、final_decisions、audit_events：记录外部调用尝试、复核人员、终局单写结果和可排序审计原因。

## 公开接口

1. POST /v1/tasks/lock：锁定一次切块种薯批，返回任务 ID、任务代次和资源租约摘要。
2. POST /v1/tasks/{id}/cut-confirmations：提交双人切块确认，要求操作号、人员 ID、当前代次和锁定摘要。
3. POST /v1/tasks/{id}/sample-seals：封存网筐样本并绑定盲码，原子推进样本封存状态。
4. POST /v1/tasks/{id}/observations/{age_day}：提交指定愈伤日龄覆盖矩阵和探头读数。
5. POST /v1/tasks/{id}/bud-eyes/verify：提交芽眼覆盖点位活性计数并进行边界校验。
6. POST /v1/tasks/{id}/disease-evidence：提交快检板孔、病斑、渗液和复判证据版本。
7. POST /v1/tasks/{id}/reviews：提交独立复核意见，校验角色分离和代次一致性。
8. POST /v1/tasks/{id}/finalize：竞争生成允许入催芽、病害隔离或任务取消终局；GET /v1/tasks/{id} 返回当前聚合、证据闭合度和确定排序原因。

## 失败边界

1. 所有写接口在单个 SQLite 事务内完成目录校验、代次校验、幂等校验、状态推进、租约写入和审计记录，失败时不得留下局部样本、租约、覆盖格、揭示记录或终局。
2. 并发锁定、换棚、启动和终局由数据库唯一约束、条件更新和事务重试裁定，公开测试使用同步屏障复现竞争。
3. 错误响应包含稳定错误码和按繁育地块、切块批号、网筐封签、愈伤日龄、检测孔位排序的确定原因。
4. 适配器调用以逻辑时钟和故障脚本驱动，拒绝、断连、超时、格式错误都进入待重试状态，重试次数和目标固定可断言。
5. 旧代次迟到读数追加为审计事件或被拒绝，不覆盖当前证据版本，也不影响当前终局判断。
6. 重启恢复从 SQLite WAL 读取任务、租约、幂等记录和证据链，恢复后的状态、重试队列和错误排序保持一致。

## 验收标准

1. 锁定时固定繁育地块、品种代次、原薯窖位、切刀消毒批次、切块批号、网筐封签、愈伤棚位、探头窗口、抽样盲码、芽眼点位、快检板孔、催芽室时隙、木栓化阈值、失水率阈值、病害阈值和任务代次。
2. 每个切块批号、网筐封签、盲码、愈伤棚位、探头窗口、催芽室时隙和检测孔位在开放任务中只能被一个有效任务占用，并发锁定、换棚或启动必须原子裁定。
3. 切块确认、样本封存、观察提交、揭盲与复核必须匹配当前任务代次；相同操作号同内容重试幂等，内容冲突报错，陈旧规则或错误窖位不得推进状态。
4. 愈伤观察必须覆盖锁定日龄和网筐集合，木栓化等级、失水块数、腐烂块数、芽眼活性和补测标记均使用确定整数规则，非法计数不得写入有效覆盖格。
5. 失水率、棚温、相对湿度、块重变化、快检 Ct 值和病斑直径全部使用固定小数位整数运算并检查长度、符号、除零和溢出，算术失败不得写入派生证据。
6. 温湿度探头、快检读板器或称重适配器拒绝、断连、超时、格式错误只能形成可审计待重试调用，重试对象、次数和逻辑时间可确定，不得生成合格结果或提前释放占用。
7. 出现木栓化不足、失水率越界、芽眼活性不足、软腐疑阳或晚疫病疑点时只能创建一个带代次的复判证据，复判须覆盖受影响日龄、网筐、盲码和孔位，旧代次迟到读数保持不可覆盖且不影响当前结论。
8. 全部愈伤日龄、芽眼、病害和理化证据闭合并满足锁定阈值，且由两名不同合格人员独立复核后，竞争成功者才可生成唯一入催芽凭据；并发入催芽、病害隔离与取消只能形成一个终局。

## 确定性测试场景

1. catalog_lock_test.go：成功锁定完整快照；繁育地块与品种代次错配拒绝；原薯窖位摘要陈旧拒绝；切刀消毒批次不匹配拒绝。
2. lease_concurrency_test.go：重复网筐封签拒绝；重复盲码拒绝；并发愈伤棚位占用只有一个成功；并发检测孔位占用失败事务不留下租约。
3. idempotency_generation_test.go：相同操作号同内容切块确认重试返回同一结果；相同操作号不同内容返回冲突；错误代次提交观察不改变状态；复核人员与切块确认人员角色重合拒绝。
4. coverage_math_test.go：日龄观察缺失拒绝；重复日龄覆盖拒绝；木栓化与失水计数不守恒拒绝；芽眼活性计数越界拒绝；定点失水率和温湿度边界通过；溢出、负值和除零拒绝。
5. adapter_retry_test.go：温湿度探头超时形成待重试调用；快检读板器格式错误不写入证据；称重适配器断连按逻辑时间重试；重启后重试队列顺序一致。
6. arbiter_final_test.go：异常筐位只创建一个当前代次复判；旧代次迟到快检读数不可覆盖；两名独立复核后可入催芽；病害隔离与取消并发竞争只保留一个终局；终态后普通推进被拒绝。
7. Public test count target：至少 6 个测试文件、18 个确定性用例，使用虚构地块、固定品种代次、窖位、网筐封签、日龄、盲码、芽眼点位、检测孔位、整数计数、定点读数、逻辑时钟和同步屏障。
8. Bug seeding target：10 个相互独立缺陷点可覆盖在租约唯一性、盲码揭示、幂等摘要、代次校验、整数运算、适配器重试、覆盖矩阵、证据版本、复核角色和终局屏障。

## 组件追踪关系

1. 种薯品种与愈伤规则目录 -> catalog 包，支撑锁定校验、阈值快照、人员资格、规则版本和陈旧摘要拒绝。
2. 切块入催芽任务聚合 -> task 包，支撑状态机、代次推进、幂等操作号、切块确认和终态拒绝。
3. 网筐样本与资源占用账簿 -> ledger 包，支撑网筐封签、盲码、棚位、探头窗口、时隙和孔位的并发租约。
4. 愈伤日龄及芽眼采集账簿 -> evidence 包的 coverage 模块，支撑日龄矩阵、芽眼点位、整数守恒和定点理化运算。
5. 病害复判及终局仲裁器 -> evidence 与 arbiter 包，支撑快检版本链、异常复判、独立复核、单写终局。
6. Go HTTP API -> api 与 cmd 包，支撑 JSON 路由、错误码、审计查询、逻辑时钟注入、Docker 运行入口。

## 独特性

本项目聚焦马铃薯种薯繁育场的切块后愈伤、芽眼活性、病害快检和入催芽前仲裁，核心难点来自网筐封签一次性闸门、多类农业现场资源租约、按愈伤日龄展开的覆盖矩阵、盲码样本揭示约束、定点理化读数和不可覆盖复判证据链，业务对象与前序项目的门窗试验、DNSSEC、通信密钥、海试航行、火箭回收、卫星测控、水稻、食用菌、可可、叶菜、蜂蜜和橄榄鲜果场景均明显不同。
